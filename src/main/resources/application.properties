# Spring Security (optional)
spring.security.user.name=shanu
spring.security.user.password=shanu

# Database
spring.datasource.url=jdbc:mysql://smarthostel-db.ctyaumk0yxyr.eu-north-1.rds.amazonaws.com:3306/smarthostel-db
spring.datasource.username=admin
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect
server.port=${PORT:8080}


# Email configuration (Gmail + app password)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=chauhanshanu600@gmail.com
spring.mail.password=${MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Frontend URL
app.frontend.url=http://localhost:3000

# JWT secret (256-bit Base64 safe key)
jwt.secret=${JWT_SECRET}

# debugging
logging.level.org.springframework.security=DEBUG



# AWS Credentials
aws.access-key=AKIATS27RRSHI3HSX3W3
aws.secret-key=${AWS_SECRET_KEY}

# AWS S3 Config
aws.region=eu-north-1
aws.s3.bucket-name=amzn-s3-bucket-profile-pictures
